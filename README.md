# C++ Math 範例

這是一個簡單的 C++ 練習專案，示範如何定義 `Math` 結構，並使用 `math.abs()` 計算整數的絕對值。

## 個人基本資料

- 姓名：請填寫姓名
- 學號：請填寫學號
- 班級：請填寫班級
- 電子郵件：請填寫電子郵件

## 檔案

- `main.cpp`：主程式

## 編譯

使用 g++ 編譯：

```bash
g++ -Wall -Wextra -pedantic main.cpp -o main.exe
```

## 執行

Windows PowerShell：

```powershell
.\main.exe
```

預期輸出：

```text
Hello, world.
5
```
